import { GoHeaderBarDirective } from './go-header-bar.directive';

describe('GoHeaderBarDirective', () => {
  it('should create an instance', () => {
    const directive = new GoHeaderBarDirective();
    expect(directive).toBeTruthy();
  });
});
